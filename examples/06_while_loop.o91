class Main is
    this() is
        var sum : Integer(0)
        var i : Integer(0)
        while i.Less(Integer(5)) loop
            sum := sum.Plus(i)
            i := i.Plus(Integer(1))
        end
        total.Print()
    end
end